function getCookie(name) {	const nameEQ = name + '=';	const cookies = document.cookie.split(';');	for (let cookie of cookies) {		cookie = cookie.trim();		if (cookie.indexOf(nameEQ) === 0) {			try {				return JSON.parse(atob(cookie.substring(nameEQ.length)));			} catch (e) {				return {};			}		}	}	return {};}function setCookie(value) {	const maxAge = 7 * 24 * 60 * 60; 